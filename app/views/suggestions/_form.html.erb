<%= form_with(model: suggestion, local: true) do |form| %>
  <% if suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% suggestion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :enter_suggestion %>
    <%= form.text_area :enter_suggestion %>
  </div>

  <div class="field">
    <%= form.label :enter_complaint %>
    <%= form.text_area :enter_complaint %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
